# Session Handoff — 2026-05-09

Read this file first in the next session.

## What was done this session

1. **Completed wiki vocab extraction** — 129/129 files. Partial work from Windows was already committed; 11 remaining files (token-truncation victims) were finished here. Output: `training_data/wiki/nouns.txt` (2790), `verbs.txt` (1250), `adjectives.txt` (629).

2. **Completed triplet_stories vocab extraction** — 40/40 files. DeepSeek run via `meta/extract_vocab.py`, Codex mop-up for 8 persistent API failures. Output: `training_data/triplet_stories/nouns.txt` (2788), `verbs.txt` (546), `adjectives.txt` (432).

3. **Created inventory/ folder** at repo root — one merged sorted vocab file per corpus source:
   - `inventory/wiki.txt` (3921 words)
   - `inventory/stories.txt` (3092 words)
   - `inventory/philosophy.txt` (1814 words)
   - `inventory/reasoning.txt` (538 words — LOW, reasoning fix needed first)

4. **Installed RTK** (Rust Token Killer v0.39.0) at `~/.local/bin/rtk`. Added RTK section to `AGENTS.md` and created `RTK.md` at repo root. No global hook — explicit prefix only.

5. **Patched `meta/extract_vocab.py`**:
   - Retries on `json.JSONDecodeError` with 15s/30s backoff (was only retrying empty responses)
   - Bumped `max_tokens` to 65536 (32k was too small for thinking model — caused `length` stop and truncated output)

6. **Fixed queue path issue** — `training_data/wiki/files.txt` and `files_done.txt` had Windows absolute paths (`D:/Ninereeds/...`). Converted to Linux relative paths.

## Immediate next steps (in order)

### 1. Fix reasoning vocab

`training_data/reasoning/vocab/verbs.txt` and `adjectives.txt` are single comma-separated lines with truncated stems. Fix manually or send to worker:

- Split to one word per line
- Correct truncated stems: `arrang→arrange`, `caus→cause`, `divid→divide`, `measur→measure`, `remov→remove`, `receiv→receive`, `increas→increase`, `decreas→decrease`
- Regenerate `inventory/reasoning.txt` after fix

### 2. Merge all into training_data/vocab/

```python
from pathlib import Path

REPO = Path('.')
sources = {
    'nouns': [...],
    'verbs': [...],
    'adjectives': [...],
}
# Merge all nouns.txt across folders → training_data/vocab/nouns.txt
# Same for verbs, adjectives
# Deduplicate + sort
```

Sources to merge:
- `training_data/philosophy/nouns.txt`, `verbs.txt`, `adjectives.txt`
- `training_data/reasoning/vocab/nouns.txt`, `verbs.txt`, `adjectives.txt` (after fix)
- `training_data/wiki/nouns.txt`, `verbs.txt`, `adjectives.txt`
- `training_data/triplet_stories/nouns.txt`, `verbs.txt`, `adjectives.txt`

### 3. Governance audit

After merge, compare `training_data/vocab/nouns.txt` etc. against `training_data/phases/wordlist.txt` to flag corpus words not taught in phases 1–6. Then human review before any rewrites.

See `memory/project_vocab_normalization_plan.md` for full governance loop design.

## Tool reference

| Task | Command |
|---|---|
| Resume any extraction | `export $(cat .env \| xargs) && python3 meta/extract_vocab.py --queue <path>/files.txt --workers 2 --timeout 300` |
| Codex mop-up | Edit `tmp/codex_extract.py` REMAINING list + OUT path, then `python3 tmp/codex_extract.py` |
| RTK | `rtk git status`, `rtk grep`, `rtk find`, `rtk read` |

## Uncommitted changes

These files are modified/new and NOT yet committed:
- `.gitignore` (added `.env`)
- `AGENTS.md` (RTK section added)
- `RTK.md` (new)
- `meta/extract_vocab.py` (retry + 65k token patch)
- `training_data/wiki/` vocab output files + converted queue files
- `training_data/triplet_stories/` vocab output files + queue files
- `inventory/` (new folder, 4 files)
- `tmp/codex_extract.py` (new)

Commit before switching to Windows.

## Branch / HEAD

- Branch: `main`
- No active background processes
