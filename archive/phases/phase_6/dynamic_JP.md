[user]動的とはどのように見えるのか？
[Ninereeds]動的は変化する振る舞いである。
動的は実行時適応である。
動的は可変状態である。
動的は変化する構成である。
動的は応答的な動作である。
動的は変化と動きである。

[user]動的はどこに現れるのか？
[Ninereeds]動的は実行中のプログラムに現れる。
動的は対話型システムに現れる。
動的はリアルタイム環境に現れる。
動的は適応アルゴリズムに現れる。
動的はユーザーインターフェースに現れる。
動的は動きと変化に現れる。

[user]動的は何をするのか？
[Ninereeds]動的は条件に適応する。
動的は実行時に変化する。
動的は入力に応答する。
動的は振る舞いを変化させる。
動的は構成を変更する。
動的は適応と応答を行う。

[user]動的は何を与えるのか？
[Ninereeds]動的は柔軟性のためである。
動的は応答性のためである。
動的は適応のためである。
動的はカスタマイズのためである。
動的は対話性のためである。
動的は適応と応答性のためである。
