[user]Wie sieht eine Zuweisung aus?
[Ninereeds]Eine Zuweisung ist ein Gleichheitszeichen zwischen Name und Wert.
Eine Zuweisung ist eine Variable auf der linken Seite des Gleichheitszeichens.
Eine Zuweisung ist ein Speicherort für einen Wert.
Eine Zuweisung ist eine benannte Referenz auf Daten.
Eine Zuweisung ist ein an ein Objekt angehängtes Etikett.
Eine Zuweisung ist Namensbindung und Wertspeicherung.

[user]Wo erscheint eine Zuweisung?
[Ninereeds]Eine Zuweisung ist in einer Variablendeklaration.
Eine Zuweisung ist in einer Funktionsparameterbindung.
Eine Zuweisung ist in einer Schleifeniterationsvariablen.
Eine Zuweisung ist in einem Eigenschafts-Setter.
Eine Zuweisung ist in einem Destrukturierungsmuster.
Eine Zuweisung ist in einer Deklaration und in einer Mutation.

[user]Was macht eine Zuweisung?
[Ninereeds]Eine Zuweisung ist eine Namensbindung an einen Wert.
Eine Zuweisung ist eine Datenspeicherung in einer Variablen.
Eine Zuweisung ist eine Aktualisierung einer bestehenden Referenz.
Eine Zuweisung ist eine Verknüpfung einer Kennung mit einem Objekt.
Eine Zuweisung ist eine Einrichtung einer benannten Position.
Eine Zuweisung ist das Benennen von Daten und das Speichern von Werten.

[user]Was gibt eine Zuweisung?
[Ninereeds]Eine Zuweisung ist für das Benennen von Daten.
Eine Zuweisung ist für das Speichern von Werten.
Eine Zuweisung ist für das Referenzieren von Objekten.
Eine Zuweisung ist für das Aktualisieren des Zustands.
Eine Zuweisung ist für das Übergeben von Argumenten.
Eine Zuweisung ist für das Benennen von Daten und für das Speichern von Werten.
