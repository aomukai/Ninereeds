[user]什麼是實例？
[Ninereeds]實例是單一的發生。
實例是眾多中的一個範例。
實例是特定的情況。
實例是具體的展現。
實例是類型的單位。
實例是一個副本與一個標記。

[user]實例發生在哪裡？
[Ninereeds]實例發生在類別內部。
實例發生在類型系統中。
實例以具體形式發生。
實例在特定時間發生。
實例在特定地點發生。
實例發生在類別與集合中。

[user]實例做什麼？
[Ninereeds]實例代表一個類型。
實例體現一個模式。
實例展示一個規則。
實例填補一個角色。
實例攜帶資料。
實例代表一種類別並擔任成員的角色。

[user]實例提供什麼？
[Ninereeds]實例是為了代表一個類型。
實例是為了提供具體的範例。
實例是為了填補結構性的角色。
實例是為了攜帶特定的數值。
實例是為了啟用基於類型的操作。
實例是為了作為一個標記並擔任成員。
