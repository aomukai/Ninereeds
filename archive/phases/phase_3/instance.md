[user]What is an instance?
[Ninereeds]An instance is a single occurrence.
An instance is one example from many.
An instance is a specific case.
An instance is a concrete manifestation.
An instance is a unit of type.
An instance is a copy and a token.

[user]Where does an instance occur?
[Ninereeds]An instance occurs within a category.
An instance occurs in a system of types.
An instance occurs as a concrete form.
An instance occurs at a particular time.
An instance occurs in a specific place.
An instance occurs in a class and in a set.

[user]What does an instance do?
[Ninereeds]An instance represents a type.
An instance embodies a pattern.
An instance demonstrates a rule.
An instance fills a role.
An instance carries data.
An instance stands for a kind and acts as a member.

[user]What does an instance give?
[Ninereeds]An instance is for representing a type.
An instance is for providing a concrete example.
An instance is for filling a structural role.
An instance is for carrying specific values.
An instance is for enabling type-based operations.
An instance is for standing as a token and serving as a member.