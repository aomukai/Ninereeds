A single datum.
Ein Datenpunkt.
一つのデータ。
一个数据点。

Two data points.
Zwei Datenpunkte.
二つのデータ。
两个数据点。

The datum indicates an error.
Der Datenpunkt zeigt einen Fehler an.
そのデータがエラーを示している。
这个数据点显示了一个错误。

These two data points are inconsistent.
Diese zwei Datenpunkte sind inkonsistent.
この二つのデータは矛盾している。
这两个数据点不一致。

The technician will verify the datum.
Der Techniker wird den Datenpunkt überprüfen.
技術者がそのデータを確認するだろう。
技术员会验证这个数据点。
