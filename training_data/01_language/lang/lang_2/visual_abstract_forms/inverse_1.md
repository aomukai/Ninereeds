One inverse.
Ein Kehrwert.
逆数が一個。
一个倒数。

Two inverses.
Zwei Kehrwerte.
逆数が二個。
两个倒数。

The inverse of two is one half.
Der Kehrwert von zwei ist ein Halb.
2の逆数は2分の1だ。
二的倒数是二分之一。

She calculated the inverse of the matrix.
Sie hat den Kehrwert der Matrix berechnet.
彼女は行列の逆数を計算した。
她计算了矩阵的倒数。

This number has no inverse.
Diese Zahl hat keinen Kehrwert.
この数には逆数がない。
这个数没有倒数。
