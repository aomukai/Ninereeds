The function assigned the result to the variable y.
Die Funktion hat das Ergebnis der Variablen y zugeordnet.
関数は結果を変数yに代入した。
函数把结果赋给了变量y。

The algorithm is assigning weights to each feature.
Der Algorithmus ordnet jedem Merkmal Gewichte zu.
アルゴリズムは各特徴に重みを代入している。
算法正在给每个特征赋值权重。

The program will assign the new path to the configuration object.
Das Programm wird dem Konfigurationsobjekt den neuen Pfad zuordnen.
プログラムは構成オブジェクトに新しいパスを代入するだろう。
程序会把新路径赋值给配置对象。
