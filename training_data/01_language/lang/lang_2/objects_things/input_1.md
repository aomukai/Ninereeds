one input
eine Eingabe
入力が一つ
一个输入

two inputs
zwei Eingaben
入力が二つ
两个输入

The system requires one input.
Das System benötigt eine Eingabe.
システムは入力が一つ必要だ。
系统需要一个输入。

I added two inputs.
Ich habe zwei Eingaben hinzugefügt.
私は入力を二つ追加した。
我添加了两个输入。

The inputs were processed.
Die Eingaben wurden verarbeitet.
入力は処理された。
输入被处理了。
