Eine Instanz.
インスタンスが一つ。
一个实例。
One instance.

Zwei Instanzen.
インスタンスが二つ。
两个实例。
Two instances.

Der Entwickler hat eine Instanz erstellt.
開発者はインスタンスを作った。
开发者创建了一个实例。
The developer created an instance.

Diese Klasse enthält zwei Instanzen.
このクラスにはインスタンスが二つある。
这个类包含两个实例。
This class contains two instances.

Die Instanz speichert Daten.
インスタンスはデータを保存する。
实例存储数据。
The instance stores data.
