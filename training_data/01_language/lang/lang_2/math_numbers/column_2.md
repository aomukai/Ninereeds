一个列。
One column.
Eine Spalte.
列が一つ。

两个列。
Two columns.
Zwei Spalten.
列が二つ。

左边那一列有名字。
The left column contains names.
Die linke Spalte enthält Namen.
左の列に名前が入っている。

她把两列合并成一列。
She merged two columns into one.
Sie hat zwei Spalten in eine zusammengeführt.
彼女は二つの列を一つに結合した。
