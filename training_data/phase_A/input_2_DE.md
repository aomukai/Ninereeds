[user]Wie sieht Input aus?
[Ninereeds]Input ist in ein Formular eingegebener Text.
Input ist von einem Sensor empfangene Signale.
Input ist in ein Programm eingespeiste Daten.
Input ist in ein Terminal eingegebene Befehle.
Input ist an eine Funktion übergebene Werte.
Input ist Text und Signale.

[user]Wo tritt Input auf?
[Ninereeds]Input tritt an Tastatureingabepunkten auf.
Input tritt an Datenerfassungsstationen auf.
Input tritt an Systemgrenzen auf.
Input tritt an Benutzeroberflächenfeldern auf.
Input tritt an Messinstrumenten auf.
Input tritt an Tastaturen und Sensoren auf.

[user]Was macht Input?
[Ninereeds]Input speist Informationen in ein System ein.
Input startet einen Prozess mit Daten.
Input stellt Material für Berechnungen bereit.
Input liefert Signale zur Verarbeitung.
Input liefert Werte für Operationen.
Input speist ein und liefert.

[user]Was gibt Input?
[Ninereeds]Input ist für das Starten von Prozessen.
Input ist für das Bereitstellen von Daten.
Input ist für das Auslösen von Operationen.
Input ist für das Liefern von Informationen.
Input ist für das Einspeisen von Systemen.
Input ist für das Starten und Einspeisen.
