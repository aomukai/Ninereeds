The server offloaded the computation to the cloud.
Der Server hat die Berechnung in die Cloud ausgelagert.
サーバーが計算をクラウドにオフロードした。
服务器把计算卸载到了云端。

The system is offloading video encoding to the GPU.
Das System lagert die Videokodierung auf die GPU aus.
システムがビデオエンコードをGPUにオフロードしている。
系统正在把视频编码卸载到GPU。

The app will offload data to the remote server.
Die App wird Daten auf den entfernten Server auslagern.
アプリがデータをリモートサーバーにオフロードするだろう。
应用会把数据卸载到远程服务器。
