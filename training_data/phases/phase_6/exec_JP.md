[user]実行はどのようなものか？
[Ninereeds]実行はプログラム命令を実行することだ。
実行はコマンドをメモリに読み込むことだ。
実行はバイナリ操作を実行することだ。
実行はシェルからプロセスを起動することだ。
実行は指定された操作を遂行することだ。
実行は実行と操作である。

[user]実行はどこに現れるか？
[Ninereeds]実行はコマンドラインインターフェースにある。
実行はシェルインタプリタにある。
実行はオペレーティングシステムカーネルにある。
実行はプログラムローダにある。
実行はランタイム環境にある。
実行はシステムとシェルにある。

[user]実行は何をするか？
[Ninereeds]実行は実行可能コードを実行する。
実行はプログラムをメモリに読み込む。
実行は現在のプロセスイメージを置き換える。
実行は新しいプロセス実行を開始する。
実行はシステムコールを呼び出す。
実行は読み込みと実行である。

[user]実行は何を与えるか？
[Ninereeds]実行はプログラム実行のためのものだ。
実行はコマンド実行のためのものだ。
実行はプロセス生成のためのものだ。
実行はコード読み込みのためのものだ。
実行は命令処理のためのものだ。
実行は実行と起動のためのものだ。
