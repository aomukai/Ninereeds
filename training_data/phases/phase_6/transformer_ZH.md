[user]Transformer 看起來是什麼樣子？
[Ninereeds]Transformer 是由編碼器與解碼器層堆疊而成。
Transformer 是連結每個詞對的注意力機制。
Transformer 是每個層位置的饋送神經網路。
Transformer 是具有層標準化步驟的殘差路徑。
Transformer 是加入輸入嵌入中的位置編碼。
Transformer 是並行架構與序列處理單元。

[user]Transformer 出現在哪裡？
[Ninereeds]Transformer 存在於大型語言模型訓練流程中。
Transformer 存在於機器翻譯編碼器-解碼器堆疊中。
Transformer 存在於文本生成推理伺服器中。
Transformer 存在於序列分類模型架構中。
Transformer 存在於自監督學習框架中。
Transformer 存在於自然語言處理與電腦視覺系統中。

[user]Transformer 做什麼？
[Ninereeds]Transformer 將輸入序列編碼為隱藏表示。
Transformer 將隱藏狀態解碼為輸出詞概率。
Transformer 在整個序列中關注相關位置。
Transformer 透過多層堆疊傳播梯度。
Transformer 將可變長度輸入映射為固定維度向量。
Transformer 計算自注意力分數並產生加權總和。

[user]Transformer 提供什麼？
[Ninereeds]Transformer 用於建模序列中的長程依賴關係。
Transformer 用於在詞位置間平行化訓練。
Transformer 用於在任務間遷移學習到的模式。
Transformer 用於生成連貫的多詞輸出序列。
Transformer 用於表示語言中的階層結構。
Transformer 用於擴展模型容量並提升預測準確度。
