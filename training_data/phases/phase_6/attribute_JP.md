[user]属性はどのように見えるか？
[Ninereeds]属性はオブジェクトに付随する特性である。
属性はクラス定義におけるフィールドである。
属性は辞書エントリにおけるキーである。
属性はデータベーステーブルにおけるカラムである。
属性はエンティティの特性である。
属性はオブジェクトの特性でありエンティティの特性である。

[user]属性はどこに現れるか？
[Ninereeds]属性はオブジェクト指向プログラミングに現れる。
属性はデータ構造定義に現れる。
属性はAPI応答フィールドに現れる。
属性はデータベーススキーマに現れる。
属性はHTML要素タグに現れる。
属性はクラス定義とデータベーススキーマに現れる。

[user]属性は何をするか？
[Ninereeds]属性はオブジェクトの状態を記述する。
属性はオブジェクトのデータを格納する。
属性はオブジェクトの特性を定義する。
属性は要素に関するメタデータを運ぶ。
属性はインスタンス間を区別する。
属性は状態を記述しデータを格納する。

[user]属性は何を与えるか？
[Ninereeds]属性は特性を記述するためのものである。
属性は特性を格納するためのものである。
属性は構造を定義するためのものである。
属性はメタデータを運ぶためのものである。
属性はオブジェクトを区別するためのものである。
属性は特性を記述し特性を格納するためのものである。
