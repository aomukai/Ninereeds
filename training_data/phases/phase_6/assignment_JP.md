[user]代入とはどのようなものか？
[Ninereeds]代入とは名前と値の間に等号を置くことである。
代入は変数が等号の左側に存在する。
代入は値を格納する場所である。
代入はデータへの名前付き参照である。
代入はオブジェクトに付けるラベルである。
代入は名前の束縛と値の格納である。

[user]代入はどこに現れるか？
[Ninereeds]代入は変数宣言に現れる。
代入は関数仮引数の束縛に現れる。
代入はループの反復変数に現れる。
代入はプロパティのセッターに現れる。
代入は分割代入のパターンに現れる。
代入は宣言とミューテーションに現れる。

[user]代入は何をするか？
[Ninereeds]代入は名前を値に束縛する。
代入はデータを変数に格納する。
代入は既存の参照を更新する。
代入は識別子とオブジェクトを関連付ける。
代入は名前付きの場所を確立する。
代入はデータに名前を付け値を格納する。

[user]代入は何をもたらすか？
[Ninereeds]代入はデータに名前を付けるためのものである。
代入は値を格納するためのものである。
代入はオブジェクトを参照するためのものである。
代入は状態を更新するためのものである。
代入は引数を渡すためのものである。
代入はデータに名前を付け値を格納するためのものである。
