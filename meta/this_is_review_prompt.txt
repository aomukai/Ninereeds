EXECUTOR PROMPT FOR DEEPSEEK — THIS-IS OPENER REVIEW

You are reviewing Ninereeds curriculum files for bad "[Ninereeds]This is ..." openers.

Read these files first:
  AGENTS.md
  training_data/phases/this_is_review_input.txt
  training_data/phases/this_is_weird.txt
  training_data/phases/this_is_the.txt
  training_data/phases/this_is_a.txt
  training_data/phases/this_is_an.txt

Task:
  Classify every row in training_data/phases/this_is_review_input.txt.

Goal:
  Separate simple opener-removal cases from files that have deeper issues.

For each input row, classify into exactly one category:

1. SAFE_REMOVE
   Meaning:
   - The file mainly has a deictic opener problem.
   - Removing the opener is likely safe after later confirmation.
   - No obvious deeper grammar/ontology problem is signaled by the opener itself.

2. BAD_DETERMINER
   Meaning:
   - The opener itself uses a bad article/determiner framing such as "the" where the concept is being introduced text-only.
   - This may indicate more than opener removal is needed.
   - Example idea: "This is the yolk."

3. DEEPER_PATHOLOGY
   Meaning:
   - The opener signals a broader grammar, ontology, pronoun, contraction, function-word, or nonsense problem.
   - Example ideas: "This is their.", "This is they're.", "This is somewhere.", "This is the dark."

Rules:
  - Do not edit any curriculum files.
  - Review only the rows listed in training_data/phases/this_is_review_input.txt.
  - You may open the referenced files to inspect context before classifying.
  - Be conservative. If a row suggests more than simple opener removal, do NOT mark SAFE_REMOVE.
  - Keep the output reproducible and compact.

Output file:
  training_data/phases/this_is_review_flags.txt

Write one tab-separated line per reviewed row in this exact format:
  CATEGORY<TAB>path<TAB>line_number<TAB>opener_text<TAB>short_reason

Allowed CATEGORY values:
  SAFE_REMOVE
  BAD_DETERMINER
  DEEPER_PATHOLOGY

The short_reason must be 3 to 10 words and concrete.

After completing the classifications, write a structured receipt to:
  training_data/phases/this_is_review_receipt.txt

Use this exact format:

RECEIPT
-------
Files processed this run: training_data/phases/this_is_review_input.txt
Progress ledger last entry: NONE
Output file record count: [count of lines in training_data/phases/this_is_review_flags.txt, read directly]
Files remaining: 0
Status: DONE | BLOCKED
Blocker (if BLOCKED): [exact reason]

If you cannot verify the output file count, report BLOCKED.
