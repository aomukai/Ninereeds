# Session Handoff — 2026-05-08 (Windows → Linux)

Read this file first in the next session.

## What was done this session

1. **Both repair queues completed**
   - Duplicate queue: 58 files — done
   - Formatting queue: 275 files — done
   - Runner: `meta/repair_formatting_opencode.py` (patched to direct OpenRouter API, no opencode)

2. **CLAUDE.md created** at repo root — tool index, platform rules, auth pattern, corpus format spec, hard constraints. Read it on startup.

3. **`meta/extract_vocab.py` written** — direct DeepSeek API runner for vocabulary extraction. Cross-platform, resumable, parallel workers.

4. **Wiki vocabulary extraction: in progress**
   - 118/129 files done at end of Windows session; 11 still pending (all token-truncation victims)
   - Output: `training_data/wiki/nouns.txt` (1737), `verbs.txt` (1234), `adjectives.txt` (582)
   - Progress ledger: `training_data/wiki/files_done.txt`
   - Remaining 11 files all failed with `length` stop — fixed by 32768 token budget in current script

## Immediate next steps (in order)

### 1. Finish wiki extraction

```bash
python3 meta/extract_vocab.py --queue training_data/wiki/files.txt --workers 4 --timeout 300
```

Check remaining count first:

```bash
python3 - <<'PY'
from pathlib import Path
q = [l.strip() for l in Path('training_data/wiki/files.txt').read_text().splitlines() if l.strip()]
done = {l.strip() for l in Path('training_data/wiki/files_done.txt').read_text().splitlines() if l.strip()}
print(f"remaining: {len([f for f in q if f not in done])}")
PY
```

On Linux, `--workers 4` should be stable. If rate-limit errors appear, drop to `--workers 2`.

**Token budget note:** DeepSeek V4 Flash is a thinking model. Reasoning tokens eat into `max_tokens` before any output is written. 16k was insufficient for larger files — stop reason `length` on OpenRouter confirms this. The script is set to `max_tokens=32768`. Do not lower it.

### 2. Extract vocab from triplet_stories

```bash
find training_data/triplet_stories -name "*.md" | sort > training_data/triplet_stories/files.txt
python3 meta/extract_vocab.py --queue training_data/triplet_stories/files.txt --workers 4 --timeout 300
```

### 3. Fix reasoning vocab files

`training_data/reasoning/vocab/verbs.txt` and `adjectives.txt` are malformed — single comma-separated lines with truncated stems (e.g. `arrang`, `caus`, `divid`). The nouns file is clean.

Fix needed: split to one-per-line, fix bad stems (arrange, cause, divide, measure, remove, receive, sell, change, decrease, increase, share). Either do this manually or send through DeepSeek with the normalisation prompt.

### 4. Merge all corpus vocab files

Once wiki, triplet_stories, and reasoning are done, merge everything into `training_data/vocab/`:

```
training_data/vocab/nouns.txt
training_data/vocab/verbs.txt
training_data/vocab/adjectives.txt
```

Each source:
- `training_data/philosophy/nouns.txt` (910), `verbs.txt` (565), `adjectives.txt` (628)
- `training_data/reasoning/vocab/nouns.txt` (536) — verbs/adj need fixing first
- `training_data/wiki/nouns.txt`, `verbs.txt`, `adjectives.txt`
- `training_data/triplet_stories/nouns.txt`, `verbs.txt`, `adjectives.txt` — not yet extracted

Merge = deduplicate + sort. Simple Python one-liner per file.

### 5. Compare against wordlist.txt

After merge, run `meta/workflow/vocabulary_audit.py` or `meta/workflow/vocabulary_gap_check.py` against the merged vocab to flag words not taught in phases 1–6. Then human review before any rewrites.

## Known issues / flags

- `training_data/wiki/wiki_1/evidence_and_justification_level2.md` was processed in an early run and logged 664 verbs / 0 nouns — obviously wrong. Flag for manual review after the merge.
- 14 wiki files failed repeatedly with JSON errors (rate limiting). They will be retried in the next run. If they keep failing, check file contents for unusual characters.

## Tool reference

| Task | Command |
|---|---|
| Resume wiki extraction | `python3 meta/extract_vocab.py --queue training_data/wiki/files.txt --workers 4 --timeout 300` |
| Start triplet_stories | `find ... \| sort > files.txt && python3 meta/extract_vocab.py --queue ...` |
| Auth | `export OPENROUTER_API_KEY=...` |
| On Linux, opencode also available | `meta/scripts/opencode_ds.sh` / `opencode_ds_fanout.sh` |

See `CLAUDE.md` for full tool index and platform rules.

## Branch / HEAD

- Branch: `main`
- No uncommitted changes expected (vocab files are not committed, they're working files)
